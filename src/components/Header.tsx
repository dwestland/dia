/* eslint-disable arrow-body-style */
import React from 'react'
import styles from '@/styles/Header.module.scss'

const Header = () => {
  return (
    <header className={styles.header}>
      <span>Dream Interpretation.ai </span>
    </header>
  )
}

export default Header
